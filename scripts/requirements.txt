# Requirements for one-off data scripts (not the FastAPI backend)
# Install with: pip install -r scripts/requirements.txt
# Then:         playwright install chromium

playwright==1.43.0
playwright-stealth==1.0.6
pillow==10.3.0
httpx==0.27.0   # already in backend, listed here for standalone use
